%h3 My Calendar Events
%table{border: 1}
  %thead
    %tr
      %th
        Title
      %th
        Description
      %th
  %tbody
    %tr{ ng_repeat: "event in events" }
      %td
        {{ event.title }}
      %td
        {{ event.description }}
      %td
        %button{ng_click: 'deleteEvent(event);'}
          Delete
