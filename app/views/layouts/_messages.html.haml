- unless flash.keys.blank?
  #flash_messages
    - flash.each do |name, msg|
      %div(class="alert alert-#{name} alert-wrapper")
        = content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String)
